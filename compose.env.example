# Copy to .env (gitignored). Also see .mise.env for POSTGRESQL_VERSION.

# --- development (compose.yml) ---
IMAGE=ubuntu-mise:dev
CACHE_VOLUME=ubuntu-mise-cache
IMAGE_USER=dev
HOST_PORT=3004
# PORT=3004

# --- production (compose.prod.yml) ---
# PROD_IMAGE=george:prod
# HOST_PORT=80
# SECRET_KEY_BASE=generate_with_bin_rails_secret
